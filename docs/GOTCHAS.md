# Gotchas (verified on this Mac)

New rows are appended here, numbered from the last row already present.

| # | Gotcha | Mitigation | Owner |
|---|---|---|---|
| G1 | **`ELECTRON_RUN_AS_NODE=1` is set in Claude Code's Bash environment.** Any `electron` launched from inside an agent runs as plain Node: `electron --version` prints the Node version, `require('electron')` returns a string, the app "silently" fails. | `scripts/dev.mjs` deletes it before spawning; `cleanEnv` (src/main/util/exec.ts) strips `ELECTRON_*` at every caller that builds a child env, including the one that supplies `host-launcher`'s `env` — the launcher itself does not filter; CLAUDE.md warns. | scripts, util/exec |
| G2 | **Electron's Node ABI (149) ≠ system Node ABI (137).** A native module built for one crashes in the other. | No native modules in main. `node-pty` only in the host under system Node. | architecture |
| G3 | **`node-pty@1.1.0` prebuilt `spawn-helper` ships without the execute bit** → `posix_spawnp failed` at first spawn. | `postinstall` chmod; `createHost()` chmods once at host startup; `hangar doctor` checks. **All three repair only the CURRENT arch** — `spawnHelperCandidates` resolves `prebuilds/${process.platform}-${process.arch}` — so on an arm64 machine the x64 helper is never touched. Re-measured 2026-09-09 in `node_modules`: `darwin-arm64/spawn-helper` **0755** (postinstall repaired it), `darwin-x64/spawn-helper` **0644**, exactly as npm laid it down. That is the row's premise still holding, and G72 is its consequence for a packaged build. | scripts/fix-node-pty, host/pty-fix, host/server |
| G4 | **GUI apps don't get the interactive shell PATH.** Non-interactive resolution here yields stale `claude` 2.0.76 + Node 22 from `/usr/local/bin`. | PTYs are `$SHELL -il`; main resolves PATH via `-ilc` with sentinels; node path cached in config. | shell-env |
| G5 | `zsh -l` (login, non-interactive) does **not** read `~/.zshrc`, so nvm is absent. Must be `-il`. | as G4 | shell-env, agent-service |
| G6 | **Type-stripped TS under Node needs `.ts` import extensions, erasable syntax only, and default imports for CJS packages** (`@xterm/headless`, `@xterm/addon-serialize`, `node-pty`). | tsconfig `erasableSyntaxOnly` + `verbatimModuleSyntax`; a test imports every `host/cli/shared` file under Node. | tsconfig, host |
| G7 | **Replaying raw PTY bytes to rebuild a terminal is wrong** (cursor-movement-heavy TUI output at a different size becomes garbage). | Headless xterm mirror + `serialize()`; resize *before* snapshot. | host/session |
| G8 | `fit()` on a zero-size/hidden container yields NaN cols/rows and a broken PTY size. | Guard on measured size; ResizeObserver; never fit while `display:none`. | TerminalView |
| G9 | macOS Unix-socket path limit is 104 bytes. | Socket at `HANGAR_HOME/run/host.sock`; the socket path's **byte** length is validated at startup against `MAX_SOCKET_PATH_LEN` (100, a margin under 104) by `assertSocketPathLength`. Do not substitute a character count on `HANGAR_HOME` — "under 60 chars" rejects working homes, e.g. a 78-char home gives a 92-byte socket path. | paths, host/main, host/server |
| G10 | A stale socket file after a host crash gives `ECONNREFUSED`, not `ENOENT`. | Launcher unlinks and respawns when pidfile pid is dead. | host-launcher |
| G11 | A detached child inherits the parent's stdio and dies with it unless `stdio` is redirected and `unref()` called; it also receives SIGHUP when the terminal closes. | `detached:true`, stdio to log fd, `unref()`; host ignores SIGHUP. | host-launcher, host/main |
| G12 | **Disk headroom is not guaranteed.** (The original "~9 GB free of 926 GB" was stale — re-measured 2026-09-07: **279 GB free**. The mitigations below are still right; the premise was wrong, so do not use "the disk is nearly full" as a reason to skip a check or cut a feature.) Worktrees + copied `node_modules` could fill it; Electron caches add more. | 2 GB preflight; APFS `cp -c` (verified) instead of copies; status-bar warning < 5 GB; never fall back to full copies. **Built — Plan 05 Task 7:** the preflight and the warning now share one set of numbers (`shared/disk.ts`), measured through `app:diskFree` on `paths.worktreesDir`. `LOW_DISK_WARN` had sat in `src/main/util/disk.ts` with no consumer since Plan 02. | disk.ts, worktree-setup, shared/disk.ts, StatusBar |
| G13 | Fresh worktrees lack gitignored files: `.env*`, `.claude/settings.local.json` (per-project permission allowlist), `node_modules`. | Project setup: copyPatterns + cloneDirs + postCreate, with those defaults. | worktree-setup |
| G14 | `git worktree add -b` fails if the branch exists; `git worktree remove` fails on dirty trees; `origin/HEAD` is often unset on old clones. | Suffixing; inspect + typed force; default-branch fallback chain. | git |
| G15 | `--add-dir` directories are **not** restored by `--resume` (*docs*). | Launch command always re-passes them. | claude-launch |
| G16 | `--settings` merges with user/project hooks rather than replacing (*docs*) — good — but a slow hook delays Claude. | `hangar event` ≤ 300 ms, exit 0 always; hook `timeout: 5`. | cli/event |
| G17 | xterm 6 addons: WebGL context loss on sleep/GPU reset; too many contexts. | `onContextLoss` → DOM renderer fallback; ≤ 4 terminals mounted. | TerminalView |
| G18 | Claude Code's UI relies on Unicode 11 widths (emoji, spinners); default xterm width tables misalign it. | `Unicode11Addon`, `activeVersion='11'`. | TerminalView |
| G19 | Shift+Enter in xterm.js sends plain `\r` (same as Enter) → submits instead of newline. | Map Shift+Enter → `\n` (documented newline in every terminal); Option+Enter via `macOptionIsMeta`. | keymap |
| G20 | ⌘-shortcuts are swallowed by xterm unless intercepted; ⌘C must not send `^C`. | `attachCustomKeyEventHandler` returns false for app combos; ⌘C copies selection or no-ops. | keymap |
| G21 | Type-ahead: writing the startup command before the shell prints its prompt is fine with zsh (verified), but the command echoes in the terminal and lands in shell history. | Accepted; visible is better than magic. Prefix with a space (`HIST_IGNORE_SPACE`) is *not* assumed. | host/session |
| G22 | Claude Code sets the terminal title (OSC 0/2) to its session name/summary; without capture the sidebar and title drift apart. | Host mirror `onTitleChange` → `title` events → pane header subtitle; `--name` seeds it with the agent's name. | host/session, PaneHeader |
| G23 | The folder-trust prompt appears on first run in each new worktree; no pre-accept mechanism (*docs*). | Toast tells the user; nothing automatic. | NewAgentDialog |
| G24 | `git` from Electron main uses the GUI env: SSH agent works (launchd), but PATH does not include Homebrew git; Apple git 2.50 at `/usr/bin/git` is fine. | Use the resolved login PATH anyway; `git` resolved from it. | shell-env, git |
| G25 | Two Electron instances on the same `userData` fight over the lock and caches. | `userData` under `HANGAR_HOME`; profiles. | paths |
| G26 | React 19 + xterm: re-rendering `TerminalView` must never recreate the `Terminal`; StrictMode double-mount in dev would attach twice. | Terminal created in a `useEffect` with a registry guard, and **`<StrictMode>` is not used anywhere** — `src/renderer/main.tsx` renders the app bare, app-wide, not just around the panes. Read the original "(or idempotent attach)" as an option that was **not** taken: the attach is **not** idempotent, measured on React 19.2.8 — one StrictMode mount sends **2** `session:attach` and **0** `session:detach`, because the first cleanup runs while its own attach is still awaiting, so `attached` is still false and the detach branch is skipped. `TerminalView.test.tsx` records that rather than fixing it; making `attach` cancellable or keyed is **OWED**. So the cost of this row is real: nobody can switch StrictMode on to find the next React bug until the attach is fixed. | TerminalView, renderer/main.tsx |
| G27 | `npm view` shows `typescript@7` and `vite@8` as "latest"; installing "latest" breaks electron-vite 5 and `@vitejs/plugin-react` peers. | Exact pins in §20; `npm install` never run with bare package names. | package.json |
| G28 | **Corrected.** Apple `cp -c` does NOT fail on a non-APFS volume — it silently degrades to a full byte copy and exits **0**. Measured with `df -k` deltas on a 40 MB file: APFS same-volume freed 108 KB (a real clone), an HFS+ destination consumed the whole 40,960 KB. So "never fall back to a full copy" (spec §10.2 step 6, and G12's disk budget) is **not** something `cp -c` gives you. | Compare `statSync(src).dev` against the destination's `dev` and refuse when they differ; never infer from the exit code. Special files can still fail, so keep the warn-and-skip path. | worktree-setup |
| G29 | Hook commands run with Claude's env; if PATH were altered, `hangar` might not resolve. | Hooks use the absolute `<repoRoot>/bin/hangar`. | claude-launch |
| G30 | Deleting a folder or moving a folder into its descendant corrupts the tree. | `folder:move` cycle check; delete re-parents children; store invariants tested. | workspace-store |
| G31 | Named imports from `@xterm/headless` / `@xterm/addon-serialize` fail under Node ESM ("Named export 'Terminal' not found"). | Default-import then destructure: `import xterm from '@xterm/headless'; const { Terminal } = xterm;`. | host |
| G32 | `bin/hangar` must resolve `node` from inside a PTY session; hooks inherit the session env. | The app passes `HANGAR_NODE=<nodeBin>`; the shim falls back to `node` on PATH. Hooks use the absolute shim path, and the shim resolves its own symlinks. | cli, claude-launch |
| G33 | **Quoting does not secure a command that is TYPED into a live shell.** The line editor consumes control bytes before the parser sees the quotes: `\x03` (send-break) or `\x15` (kill-line) in an interpolated value discards the opening quote, and a following `\r` submits the remainder as a fresh command. Verified with a real PTY; no `sh -c` round-trip test can detect it. | Strip `[\u0000-\u001f\u007f]` as well as quoting. **Built:** `shared/agent-name.ts` (`cleanAgentName`) is the one place a name is cleaned, and `hangar rename` uses it. **Built — Plan 02:** `composeStartupCommand` (`src/main/services/claude-launch.ts`) is `claude ${shellJoin(args.map(stripControlChars))}`, so every argument is stripped before it is quoted. The command word `claude` stays bare (G34). | shell-quote, agent-name, claude-launch (Plan 02) |
| G34 | Quoting a command *word* suppresses **alias** expansion in zsh and bash, so a user whose `claude` is an alias gets `command not found` when it is quoted — while `hangar doctor`'s `command -v claude` still reports it present. A shell **function** resolves fine either way; only aliases are affected. | Quote arguments, never the command word. | claude-launch |
| G35 | `tsc` cannot enforce G6. Under `moduleResolution: "bundler"` an extensionless relative import and a named import from a CJS package both typecheck clean and both throw under raw Node. | `shared/module-load.test.ts` imports every `shared`/`host`/`cli` file in a child Node process. It is the only gate. | shared/module-load.test |
| G36 | The **root** `package.json` must never gain `"type": "module"` — sandboxed Electron preloads must be CommonJS. | The per-directory `{"type":"module"}` markers in `shared/`, `host/` and `cli/` are what make Node run those as ESM. | package.json |
| G37 | `@xterm/headless`'s `Terminal.write()` is **asynchronous**: `serialize()` in the same turn returns without the data. An attach snapshot taken right after a burst silently omits its newest lines. | `Session.snapshot()` awaits `write('', cb)` before serialising. | host/session |
| G38 | node-pty's `kill()` signals only the shell's pid. SIGHUP works because the shell forwards it; SIGKILL cannot be caught, so a dying shell forwards nothing and its children are reparented to pid 1 and survive. The process group is the wrong unit too — a job-controlled child has its own. | `Session.killTree()` snapshots the tree from `ps` *before* killing, then kills deepest-first. Agent deletion runs `git worktree remove` on this guarantee. | host/session |
| G39 | An empty-string environment variable is not "unset" to `??`, and an unset variable expands to exactly that in a shell wrapper or a spawn-built env. `HANGAR_SOCKET=""` reached `net.createConnection('')`, which throws **synchronously**. | `cli/context.ts` treats empty strings as unset; `requestOnce` never rejects. | cli/context, cli/socket |
| G40 | macOS `os.tmpdir()` is a long `/var/folders/…/T` path, so any Unix socket inside it exceeds the 104-byte limit and the host dies with a bare `EINVAL`. | Tests use `test/fixtures/tmp.ts`, which mkdtemps under `/tmp`. Do not "tidy" it into `tmpdir()`. | test/fixtures/tmp |
| G41 | `vitest@5` has no `environmentMatchGlobs` (removed in v4), and `passWithNoTests` is ignored inside a `projects` entry. | Use `test.projects`; keep `passWithNoTests` at the root. | vitest.config |
| G42 | `MAX_FRAME_CHARS` and `MAX_LINE_CHARS` count UTF-16 code units, not bytes, so the real wire budget is up to ~3x the named figure for non-ASCII. | Deliberate — the caps bound host *memory*. Frame slicing is surrogate-pair-safe, because Claude Code's Ink UI emits emoji and half a pair corrupts the mirror. | shared/constants, host/session |
| G43 | An **interactive** shell ignores SIGTERM, and that is how Node's `execFile` `timeout` kills. So the timeout is inert for exactly the `$SHELL -il` that G5 forces on us: measured, a `.zshrc` running `sleep 45` returned after 45,554 ms under a 3,000 ms timeout (non-interactive control: 3,004 ms). The rc file does not die slowly — it runs to completion, so there is no bound at all. | `ExecOptions.killSignal`, passed as `'SIGKILL'` only by `shell-env.ts`. Not a global default — see G44. | src/main/util/exec, src/main/services/shell-env |
| G44 | SIGKILL on a timed-out **git** strands `.git/index.lock`. Git registers a sigchain handler that unlinks its locks on SIGTERM, and SIGKILL cannot be caught, so every later git call in that worktree dies with `Unable to create '.git/index.lock': File exists`. Reproduced. | `killSignal` defaults to SIGTERM and is opt-in per call. A Hangar worktree is one nobody opens a terminal in, so a wedge there surfaces much later as "git mysteriously fails in this agent". | src/main/util/exec |
| G45 | `$SHELL -ilc` is a **login** shell, so it runs `.zlogout` **after** the command. No fixed position in the output is safe: a `.zshrc` printing a sentinel beats `indexOf`, and a `.zlogout` printing one beats `lastIndexOf` — measured, returning `/evil/node` and `source: 'shell'`. | A per-invocation `randomBytes` nonce in the sentinel (`shared/shell-probe.ts`). It is not a trust boundary — an rc file can read the nonce from `$ZSH_EXECUTION_STRING` — it buys immunity to banners, greetings and collisions. | shared/shell-probe |
| G46 | `printf '\x1f'` is a bash/zsh extension. `dash`'s builtin emits `a\x1fb` literally and `/usr/bin/printf` emits `ax1fb`; only octal `\037` is POSIX and produced the `1f` byte under all five implementations tested. | `FIELD_SEP_PRINTF = '\\037'`. Write the separator itself as `'\u001f'`, never as the raw byte — a literal 0x1f renders as nothing in every diff and review. | shared/shell-probe |
| G47 | `command -v node` prints the bare word `node`, not a path, when `node` is a shell **function** — the standard lazy-nvm wrapper. True in zsh **and** bash (`command -v` is not `type`). Accepting it meant `execFileSync('node', …)` PATH-resolved against the wrong process, landing on `/usr/local/bin/node` v22, which cannot parse type-stripped `.ts` at all. | A non-absolute result is treated as absent, in JS so it holds for any shell. `/node$/` matches the bare string, so absoluteness must be asserted explicitly. | src/main/services/shell-env, cli/commands/doctor |
| G48 | A stray control character inside a string literal passes **vitest, tsc and eslint** silently. A null byte typed into a template literal in `workspace-ops.ts` left all three green while making `grep` and `diff` treat the file — and the plan doc it was spliced into — as **binary**, and `file(1)` report it as `data`. **And the obvious scan for it is itself broken**: `LC_ALL=C grep -P '\x00' f` exits **1 with no output** on a file that *does* contain a NUL, because grep classifies the file as binary and suppresses the match — so it reports clean. Measured on this machine against a planted NUL; `-c` and `-l` fail the same way. Reports of "NUL scan clean" using that form across Plans 03-05 were therefore vacuous (the tree was re-scanned properly afterwards and is genuinely clean). | Scan for NULs with **`grep -a`** (`LC_ALL=C grep -a -c -P '\x00' f` → count, exit 0) or, unambiguously, a byte check: `python3 -c "import sys;b=open(sys.argv[1],'rb').read();print(b'\\x00' in b)" f`. For the wider control-character class use `LC_ALL=C tr -d '\11\12\40-\176\200-\377' < f \| wc -c`, which has no binary-suppression behaviour. If `diff` says "Binary files differ" where a text diff belongs, that is the same bug announcing itself. | any source file |
| G49 | `git for-each-ref --format=%(refname:short) refs/remotes` renders `refs/remotes/origin/HEAD` as bare **`origin`**, not `origin/HEAD`. Filtering the short name for `/HEAD` therefore never matches, and the remote itself comes back as though it were a branch. | Filter on `%(refname)` (the full ref) and strip `refs/remotes/` afterwards. | src/main/services/git |
| G50 | `git worktree remove` without `--force` refuses only on a **dirty working tree** — uncommitted or untracked files. It does **not** refuse an unmerged branch: a clean worktree whose commits are not in the base branch removes with exit 0. | Check `unmergedCount` separately if you need that guarantee; never infer it from a failed remove. Verified on git 2.50.1. | src/main/services/git |
| G51 | **Authoring a file that must contain a literal `\uXXXX` escape is hazardous from an agent tool call.** Typing the escape directly into an Edit parameter or a Bash heredoc can get it decoded into a raw control byte before it reaches disk — or rejected by Bash's own control-character guard — which is G48's failure one layer up, in the tool transport rather than the source. | Build the escape at runtime instead of typing it: in Python, `chr(92) + 'u0003'`. Then verify with the `LC_ALL=C tr` scan from G48. | any source file |
| G52 | A socket with **no `'data'` listener and no `setEncoding`** stays paused. With buffered unread data it never emits `'end'`, and because `allowHalfOpen:false` drives auto-destroy from `'end'`, it never emits **`'close'`** either — so `await new Promise(r => s.on('close', r))` hangs forever. Measured as a deterministic 20 s vitest timeout, not a flake. | Drain the stream: `s.resume()`, or `setEncoding('utf8')` plus a `'data'` handler. Every other socket consumer in this repo already does. | any socket client |
| G53 | TypeScript narrows a `let` from its **initializer** and does not account for an assignment made inside a **callback** in the same scope. `let e: Err \| null = null; child.on('error', x => { e = x; }); if (e !== null) e.code` fails with `Property 'code' does not exist on type 'never'` — while running correctly, so only `tsc` catches it. Copying through an annotated local does not help: the read is already narrowed. | Store a pre-formatted value with no union to narrow (`let reason = ''`), or widen the initializer with an explicit `as`. | any callback-assigned local |
| G54 | Mixing local and UTC `Date` accessors (`getHours()` with `getUTCMinutes()`) is invisible under the suite's `TZ: 'UTC'` pin, because local and UTC minutes always agree there. It breaks only in **half-hour** zones: 09:05Z renders as `14:05` instead of `14:35` in Asia/Kolkata. | Never mix the two families in one expression. A test that overrides `process.env.TZ` to a half-hour zone is the only thing that catches it. Second occurrence on this project — `shared/relative-time.ts` had the same shape. | any Date formatting |
| G55 | `npm i pkg@1.2.3` does **not** pin — npm's default `save-prefix` writes `"^1.2.3"` into `package.json`, so a step labelled "pinned" produces ranges. The lockfile masks it until someone installs without one. | `npm i --save-exact`. **Which specifiers get a range is a deliberate split, not an oversight**, and spec §20 is the authority: `electron@44.2.0`, `electron-vite@5.0.0` and every renderer package are **exact** — those are the ones whose float moves Chromium, the Node ABI or the React runtime. `vite@^7.3.6`, `typescript@^5.9.3` and `vitest@^5.0.0` keep ranges because §20 pins them to a **major line** with a stated reason (electron-vite 5 peers vite ^5\|\|^6\|\|^7; the TS 7 native-preview compiler is unsupported). CLAUDE.md rule 6 says the same thing in prose. | package.json |
| G56 | The renderer's CSP is `script-src 'self'` with no `'unsafe-inline'`, and `@vitejs/plugin-react` injects an **inline** react-refresh preamble in dev. It works only because Vite injects that preamble **above** the CSP `<meta>`, and a meta-delivered policy governs only content parsed after it. | Do **not** move the CSP `<meta>` to the top of `<head>` — the obvious tidy-up silently breaks the dev renderer while the production build keeps working. | src/renderer/index.html |
| G57 | **`osascript -e 'tell application "System Events" ... keystroke "q" using command down'` quits whatever is FRONTMOST, not the process you named.** `keystroke` posts a synthetic event to the window server's current key target; naming a process in the `tell` block does not retarget it, and it does not raise that process first. Measured on this machine: an attempt to quit the Electron dev window closed the editor the agent was running inside, killing the in-flight subagent with it. | Never use synthetic keystrokes to control a specific app. Quit by PID (`kill -TERM <pid>` on the pid you spawned) or, for a real app, `osascript -e 'tell application "X" to quit'` — which is addressed, not positional. To screenshot the window, drive Chromium's CDP (`--remote-debugging-port`, then `Page.captureScreenshot`) rather than macOS `screencapture`, which additionally needs a Screen Recording TCC grant the terminal usually lacks. | any GUI automation |
| G58 | **Vitest transforms `.tsx` with esbuild, which reads the ROOT `tsconfig.json` — never `tsconfig.web.json`.** The root file sets no `jsx` field (`"jsx": "react-jsx"` lives in the web config), so esbuild falls back to the CLASSIC transform and every `<Component />` in a `.test.tsx` becomes `React.createElement`, failing at run time with `ReferenceError: React is not defined`. `tsc -p tsconfig.web.json --noEmit` stays green throughout, and the app build is unaffected because `electron.vite.config.ts` runs `@vitejs/plugin-react`, which sets the automatic runtime itself. | `esbuild: { jsx: 'automatic' }` on the renderer project in `vitest.config.ts`. Do not "fix" it by adding `jsx` to the root tsconfig — that config is shared with the node project. | vitest.config |
| G59 | **A React component that subscribes to a zustand 5 store must return a referentially stable value from its selector, or it renders forever.** zustand 5 hands the selector to `useSyncExternalStore`, which re-runs it after every commit and commits again whenever the identity differs; `buildRows(...)`, `.filter(...)`, `.map(...)`, `?? []` and object literals all allocate. Measured twice on this project (Task 2's `useSession`, then reproduced deliberately in `Sidebar.test.tsx`): ~55 renders, then `Maximum update depth exceeded`. **`tsc`, `eslint` and pure unit tests are all green while it is live** — only a real React root catches it. | Select the stable value, derive after subscribing (`useMemo`). Memoise stand-ins per key (`sessions.ts`) or hoist empty literals to module scope (`EMPTY_PROJECTS`). Every store-consuming component needs a jsdom test that counts commits, with a deliberately-looping control alongside it to prove the probe is not blind. | any renderer component |
| G60 | **A jsdom test that mounts a component and asserts its own behaviour cannot see an ANCESTOR handler stealing the event.** React events bubble, and the LAST handler to run wins when both write to the same store. Measured in the built app against a seeded `~/.hangar-dev`: right-clicking any sidebar row opened the sidebar *background* menu (`rootMenuItems()`), because `Sidebar`'s `<aside>` wraps the whole tree and carries its own `onContextMenu`, while the row handlers called `preventDefault()` but not `stopPropagation()` — the row's `showMenu(...)` ran first and the ancestor's overwrote it. Every per-row action (rename, delete, open in pane, stop) was unreachable by the gesture the spec expects. 628 tests across 49 files were green, including 8 that mounted this exact sidebar. `preventDefault()` is not `stopPropagation()`. | The handler that opens a menu owns the event: call `stopPropagation()`. Test it by dispatching a REAL bubbling event (`new MouseEvent('contextmenu', { bubbles: true })`) at the deepest node — the text the pointer is actually over — through the whole mounted `App`, then assert on the store; plus one test that the background case still works, so the fix cannot become "delete the ancestor handler". Sweep siblings for the same shape: it was also live on `dragover`, where a row's refusal bubbled to `Tree`'s container and was re-marked as an allowed drop. | any renderer component with a context menu |
| G61 | **A `?? []` (or `?? {}`) fallback in a zustand selector is a LATENT G59 loop — it allocates only on the nullish path.** `useWorkspace((s) => s.snapshot?.workspace.projects ?? [])` returns the stored array once a snapshot exists and creates nothing, so a render-count test with populated state passes. Measured: mutating a stable hook to this form left **all 62 dialog tests green**, while a genuinely allocating selector (`.filter(() => true)`) failed 27. The loop is real but only reachable before bootstrap answers — e.g. ⌘N pressed before `workspace:get` returns. | Hoist the fallback to a module-level frozen constant (`EMPTY_PROJECTS`, as `workspace.ts` already does) so both paths are stable. And write at least one render-count test that mounts with **no snapshot at all** — populated-state tests cannot see this class of bug. | any zustand selector |
| G62 | **A `<dialog>` opened imperatively with `showModal()` does not honour React's `autoFocus`, and jsdom cannot catch it because it implements no `HTMLDialogElement` methods at all.** Two independent failures stack. React does not render an `autofocus` *attribute* — it calls `.focus()` during commit — and at commit time the dialog has not been shown, so it is `display: none` and the call is a no-op: measured in Chrome 152, `input.focus()` before `showModal()` left `activeElement` on **BODY**. `showModal()` then runs the HTML *dialog focusing steps*, which look for the `autofocus` **attribute** (never present) and otherwise take the **first focusable descendant** — `Dialog.tsx`'s header **Close** button. Measured: `{"open":true,"active":"BUTTON/Close","hasAutofocusAttr":false}` in the built app over CDP, and `A.afterShowModal.active = "BUTTON#a-close"` in a reduction of the same DOM. A focused `<button>` is activated by **Space**, so typing a name swallowed the first word break and shut the dialog: one Space keyUp with Close focused fired its click and left `dialog.open === false`. 913 tests across 58 files were green, 62 of them mounting these exact dialogs. | Focus **after** `showModal()`, from the one place that calls it: `ui/Dialog.tsx` focuses `initialFocus?.current ?? <first focusable in the BODY>` on the line after `d.showModal()`. Measured working: `C.afterExplicitFocus.active = "INPUT"`. Move the header Close button **last** in the DOM (positioned back into the header with `absolute`; CSS `order`/`flex-row-reverse` do **not** change tab order, only the DOM does) — on its own that also fixes it: `D.afterShowModal.active = "INPUT#d-name"`. A literal `autofocus` attribute works too (`B.afterShowModal.active = "INPUT#b-name"`) but React will not emit one. **jsdom proves none of this**: `test-setup.ts`'s `showModal` stub is an attribute flip that never moves focus, and jsdom has no keyboard activation behaviour, so a test must install a focusing `showModal` itself and say plainly that it is a simulation (`dialogs.test.tsx` → `initial focus`). | ui/Dialog.tsx, any `<dialog>` |
| G63 | **`readFileSync` refuses any file over 2 GiB** (`ERR_FS_FILE_TOO_LARGE`, measured on Node 22/24) — the buffer cap, not a permissions or path problem. Reading "just the first N bytes" as `readFileSync(p).subarray(0, n)` therefore throws on exactly the files most likely to need truncating: a runaway agent log, a checked-in model artefact, a core dump. Tested with a 2.5 GB **sparse** file, which costs 0 bytes on disk (`du -h` reports `0B`) so the fixture is free. | Open the file and read a bounded head into your own buffer (`openSync` + `readSync`), never whole-file-then-slice. `src/main/services/fs-browse.ts`'s `readHead` is the reference implementation. | any bounded file read |
| G64 | **A mutation matrix can report "nothing bites" because the test runner never ran.** A harness invoking `vitest --reporter=basic` (removed in vitest 5) exits non-zero before running a single test; a loop that only greps the output for `FAIL` then scores every mutant as *survived* — the exact inversion of the truth, and it looks like a damning result rather than a broken tool. Measured: 22 guards all reported unbitten, all 22 actually bite. | Assert the runner's own health first: a mutation loop must verify the **unmutated** baseline PASSES and that a deliberately-broken control FAILS, before trusting any survivor. Score on the exit code and the parsed pass/fail counts, never on a substring of stdout. | any mutation testing |
| G65 | **`<StrictMode>` only double-invokes EFFECTS when it is the outermost element handed to `root.render()`.** The obvious spelling of a commit-counting StrictMode test — wrap the component, then wrap that in the `<Profiler>` that counts commits — silently switches half of StrictMode off. Measured on React 19.2.8, one root each, with a mount effect and a state updater counting their own calls: `root.render(<StrictMode><A/></StrictMode>)` → effects 2, updaters 2, but `root.render(<Profiler><StrictMode><div><B/></div></StrictMode></Profiler>)` → effects **1**, updaters 2. On this project it hid a live survivor in Plan 04 Task 4's mutation matrix (the file tree's in-flight fetch dedupe, which only a double-invoked mount effect can exercise) while the test reported green. | Put the commit counter INSIDE the StrictMode shell, never above it — `FilesTab.test.tsx`'s `mountStrict` is the reference, now duplicated in `Sidebar.test.tsx`, `status.test.tsx`, `Drawer.test.tsx`, `PaneGrid.test.tsx` and `dialogs.test.tsx`, all five of which had the bad spelling. `components/strict-mode-nesting.test.tsx` re-measures both shapes on every run and is the control that proves the counter can tell them apart. A StrictMode test that claims to cover double-mounted effects must assert something only the second invocation can break. | any renderer test |
| G66 | **jsdom implements no focusability rule at all, so `focus()` + `activeElement` cannot test whether an element is focusable.** Measured on jsdom 30: `focus()` on a bare `<div>` with no `tabindex` moves `document.activeElement` to it, and so does `focus()` on a CodeMirror content div that is `contenteditable="false"` with no tab index — both of which are inert in Chromium. An assertion that a fix "made the element focusable" therefore passes with or without the fix. Same family as G62, where jsdom's missing `HTMLDialogElement` behaviour hid a real focus bug from 913 green tests. | Assert the ATTRIBUTE that makes it focusable (`tabindex`) with a control that shows the attribute is absent without the fix, and say plainly in the test that real focus behaviour is a CDP question. | any renderer test |
| G67 | **`electron-vite build` leaves the bundles UNMINIFIED (56k+ lines), but esbuild's transform STRIPS COMMENTS, so source prose does not ship.** Both halves are measured. The trap this row originally recorded — "37 occurrences of our own comment prose in the entry chunk" — was a **bad measurement**, and the mistake is the useful part: the grep was `grep -c "Measured\|measured"`, and every hit was CodeMirror's own `MeasuredHeight`/`MeasuredHeights` **identifiers**. Re-measured with phrases that cannot be symbols ("Maximum update depth", "the line editor eats", "55 renders"): **0 hits in the renderer entry chunk and 0 in `out/main/index.js`.** A task adding 12,396 bytes of comments shipped none of them. | Never attribute a bundle delta to comments — it is code. To size a dependency, **isolate and rebuild**: stub its import, build again, diff the two figures (that is how `@xterm/addon-search` was measured at 42.77 kB of a 48.02 kB delta). And when grepping a bundle for your own prose, pick a phrase that cannot also be an identifier, or you will measure a library's symbols and believe yourself. | electron.vite.config.ts |
| G68 | **Claude Code's auto-memory directory is keyed by the canonical GIT REPOSITORY ROOT, not the cwd — and the CLI's own settings description says otherwise.** The `autoMemoryDirectory` description in the 2.1.266 bundle reads *"When unset, defaults to `~/.claude/projects/<sanitized-cwd>/memory/`"*, but the code computes `canonicalWcRoot ?? gitRoot(cwd) ?? cwd`, and a linked worktree's canonical root is resolved back to the MAIN checkout (`.git` pointer file → `commondir` → `gitdir` back-check). So worktrees of one repo share a memory directory, and a design that assumes per-worktree memory — or that reads the description and assumes per-cwd — is wrong in both directions. Two further traps in the same machinery: the key encoder is `replace(/[^a-zA-Z0-9]/g,"-")` **but is truncated at 200 characters with a base36 hash of the original path appended**, which nothing outside the bundle can reproduce; and `autoMemoryDirectory` set in a checked-in `.claude/settings.json` (`projectSettings`) is **ignored for security**, so it only takes effect from `--settings` (`flagSettings`), user settings or a managed policy. | Read the bundle, not the help text or the schema prose — `claude --help` does not list this and the description contradicts the code. Hangar passes a per-agent `--settings` file (`writeAgentSettings`, `claude-launch.ts`) and treats sharing as an opt-in FALLBACK for the case where the worktree back-checks fail, not as the mechanism. Probe through an interactive login shell (G4/G5) or you will read a stale binary. | claude-launch, spec §11.9 |
| G69 | **electron-builder copies the ENTIRE production dependency tree into the asar no matter what `files` says.** `files` and `node_modules` are matched by two different code paths: `getMainFileMatchers` *appends* `!**/node_modules/**` to whatever patterns you wrote, and a separate `getNodeModuleFileMatcher` then walks `package.json`'s `dependencies` and copies them, taking only the `!` patterns from `files` into account. So `files: [out/**, package.json]` reads like "just the bundles" and shipped 59 MB — react, react-dom, zustand, every `@codemirror/*` and `@lezer/*`, `lucide-react`, `@xterm/*` — all of them already inlined into `out/renderer` by the bundler, plus `node-pty`, a native module built for the SYSTEM Node's ABI, sitting on the Electron side where CLAUDE.md rule 3 forbids it. Measured: `Hangar.app` 451 MB → **325 MB** and the asar 59 MB → 4.2 MB once excluded. | Make the Electron side genuinely self-contained (see G70), then add an explicit `- '!node_modules/**'` to `files`. Verify by listing the asar, not by reading the config: `npx asar list …/app.asar \| grep -c node_modules` must be 0. | electron-builder.yml |
| G70 | **electron-vite overrides two Vite defaults silently, and one of them decides whether a packaged app can start.** It sets `build.externalizeDeps` **true** for main and preload (Vite has no such option) and `build.minify` **false** for all three (Vite's default is `'esbuild'`). Externalising means every entry in `dependencies` that main imports becomes a bare `require()` in `out/main/index.js` — measured: `require("zod")`, from `shared/ipc-schemas.ts` — which resolves in dev because the repo has `node_modules` next to it, and in a packaged app only if you also ship one. Nothing in `npm run build`, `tsc`, `eslint` or the test suite sees this; the app opens or it does not. | Set `externalizeDeps: false` for main and preload unless a dependency genuinely cannot be bundled (a native module — which must not be in main at all, rule 3). Cost measured at +180 kB in `out/main/index.js`. Assert it rather than trusting it: `scripts/package.mjs` fails the package build if `externalRequires()` finds anything but `electron` and `node:*` in either bundle, and `scripts/packaging.test.ts` tests that function. State `minify` explicitly too, so an electron-vite upgrade cannot change the artefact by changing its own default. | electron.vite.config.ts |
| G71 | **The Node half of a packaged Hangar is verified by RUNNING it, not by listing the files.** `npm run app` exits 0 on an app whose session host cannot start — a missing `node_modules` entry, a `node-pty` rebuilt for Electron's ABI (G2), a lost execute bit (G3) — because nothing in the build ever loads what it just copied. Two adjacent measurements from this build: electron-builder 26.16.1 **preserves** modes on `extraResources` (`bin/hangar` and `spawn-helper` both arrive 0755, so the plan's chmod step repairs nothing today), and node-pty's `prebuilds/` is 65 MB of which 58 MB is Windows binaries. | `HANGAR_SMOKE_ROOT=<app>/Contents/Resources/app node scripts/smoke-host.mjs` — the existing smoke test, pointed at the packaged tree. It spawns the packaged host under the system Node, opens a real PTY (proving the binding's ABI and the spawn-helper's mode), resolves `bin/hangar` through a login shell, and reattaches after a reconnect. **The root must be ABSOLUTE** (`HANGAR_SMOKE_ROOT="$PWD/dist/…/app"`): the smoke prepends `$HANGAR_SMOKE_ROOT/bin` to the PTY's `PATH` and runs that PTY with cwd `/tmp`, so a relative root finds no `hangar` and fails with a false `timeout waiting for rename event` (measured, Plan 06). For the window, drive CDP (`--remote-debugging-port`, `Runtime.evaluate` + `Page.captureScreenshot`) against a throwaway `HANGAR_HOME` — never `osascript` (G57). | scripts/package.mjs, scripts/smoke-host.mjs |
| G72 | **A repair keyed on `process.arch` fixes only the arch that RAN it, so a cross-arch artefact ships unrepaired — and the repair then moves to first run, where it needs the install to be writable.** electron-builder 26.16.1 preserves `extraResources` modes faithfully (G71), which is the good news and the trap: it copies what `node_modules` actually holds. Measured in `dist/mac-arm64/Hangar.app/Contents/Resources/app` on 2026-09-09 — `prebuilds/darwin-arm64/spawn-helper` **0755**, `prebuilds/darwin-x64/spawn-helper` **0644**, `bin/hangar` **0755**. The arm64 helper is 0755 only because `postinstall` chmod'd it **on this arm64 machine**; nothing has ever chmod'd the x64 one (G3). Both arches are shipped on purpose (P5-8d). **Not measured, and stated as inference:** on an Intel Mac the 0644 helper is the one `spawnHelperCandidates` resolves, so the first-run `createHost()` chmod is what would save it, and that needs write access to the installed bundle. `ensureExecutable` already refuses to throw and records the EPERM in `PtyFixResult.failed`, so the failure mode is a `hangar doctor` line rather than a crash — but no x64 machine has ever run this app. | Repair every candidate arch at PACKAGE time, not just the running one, if an x64 or universal build is ever shipped; until then treat x64 as unverified and see `docs/RELEASE-CHECKLIST.md`. | host/pty-fix, scripts/package.mjs |
| G73 | **A `Record<Union, T[]>` is NOT an exhaustiveness guard — `[]` satisfies it.** The project uses `Record<KeymapAction['kind'], boolean>` twice to force a new action to be classified, and the same trick was reached for to force a new action to be *described* for the ⌘/ cheatsheet. It does not carry that weight on its own: `tsc` demands the KEY and says nothing about the value, so `'fake-twelfth': []` typechecks clean and the action silently vanishes from the panel — a cheatsheet that omits a shortcut, which is worse than no cheatsheet. Measured on this tree: with the union widened by one member and no other change, `tsc -p tsconfig.web.json --noEmit` exits **2** with **3** `TS2741`s (`SHORTCUTS`, `WORKS_IN_TEXT_FIELD`, `OWNED_BY_CODE_EDITOR`); adding `'fake-twelfth': []` to close them leaves exit **0** unless the value type refuses it. | Type the value as a NON-EMPTY tuple — `readonly [T, ...T[]]` — which turns the loophole into `TS2322: Type '[]' is not assignable`. Measured: exit **2**, 1 error. Keep the run-time half too (a test asserting every key maps to a non-empty list), because the tuple type is erased and a later `Record<..., T[]>` regression is invisible to `tsc` at the call sites. | shared/renderer, any `Record` used as an exhaustiveness gate |
| G74 | **`claude --allowedTools <tools...>` is VARIADIC: it swallows a trailing positional prompt as one more tool name.** Measured on claude 2.1.272 while prototyping Plan 06: `claude -p … --allowedTools mcp__linear__get_issue "<prompt>"` fails with `Error: Input must be provided either through stdin or as a prompt argument`. `--mcp-config` is variadic too. | Put the prompt on **stdin** (`exec`'s `input`), put `--allowedTools` LAST, and follow `--mcp-config` with another flag. `triageArgs` in `linear-triage.ts` pins the order in a test. | linear-triage |
| G75 | **`--allowedTools` PRE-APPROVES tools; it does not RESTRICT a run to them.** Measured on claude 2.1.272 (haiku, `env -i`, with a throwaway `--settings '{"permissions":{"defaultMode":"bypassPermissions"}}'` standing in for an owner whose settings bypass permissions): `--allowedTools <two Linear tools>` alone, told to "use Bash to touch <file>", created the file; `--tools ""` removed the built-in tools but left every Linear MCP tool, writes included; `--tools "" --allowedTools <two>` still ran an unlisted Linear tool (`get_user`). Only adding `--permission-mode dontAsk` denied it (`permission_denials: ['mcp__linear__get_user']`) while the listed tools kept working. | Pass all three together — `--tools "" --permission-mode dontAsk --allowedTools <list>` — and see G76, because even that is not sufficient. `--tools ""` is ONE empty argv element: fine through `execFile`, silently lost if the argv is ever joined into a shell string. | linear-triage |
| G76 | **An allow rule in ANY loaded settings source re-opens a `dontAsk` tool limit — and turning settings off turns your MCP servers off with them.** Measured on claude 2.1.272 (haiku, `env -i`): an `allow: ["mcp__linear__get_user"]` rule plus G75's three flags → `get_user` ran, and the owner's `~/.claude/settings.json` is such a source unless it is stopped. `--setting-sources ""` stops user, project and local settings loading, but on its own also dropped the user-scope MCP servers (init event: 0 tools, `mcp_servers: []`). `--setting-sources "" --strict-mcp-config --mcp-config '<one server>'` brought back exactly that server, reusing its existing OAuth login, and the unlisted tool was denied. Flag-level `--settings` still applies (an allow rule there ran), and so do managed policy settings. | `--setting-sources "" --strict-mcp-config --mcp-config <only what the run needs>`, no `--settings`, plus G75's three flags. Know what else goes: hooks, `permissions.defaultMode`, and — because they live in settings files — a settings-file `apiKeyHelper`, the `env` block and `awsAuthRefresh` (not measured individually), so a user signed in only through those gets an unsigned run. The server NAME in the config is part of every tool name (`mcp__<server>__<tool>`), so it must match the allow list. | linear-triage |
| G77 | **Node's `execFile` `signal` ignores `killSignal` — an abort sends plain SIGTERM — and the promise rejects BEFORE the child has exited.** Measured on Node 24.15: a child that ignores TERM, run with `killSignal: 'SIGKILL'` and aborted, was still alive a second later. A second trap in the fix: execFile's callback runs synchronously INSIDE the abort dispatch, before later listeners, so removing a not-yet-run abort listener from the callback cancels it. | Add an `abort` listener of your own that sends `child.kill(killSignal)` (skip it when `pid` is undefined — the child never spawned); do not remove it from the callback once the signal has aborted; for a signal already aborted at call time, kill on the next tick. Never read the rejection as "the child is gone". `src/main/util/exec.ts` is the reference. | src/main/util/exec |
| G78 | **An animated window maximise in Electron fires a burst of `resize` events that report an intermediate, NOT-maximised window; and a full-screen window reports `isMaximized()` false.** Measured on Electron 44.2.0: ~40 `resize` events over ~400 ms, each not maximised at an in-between size, while `resized`, `maximize` and `unmaximize` fire once at the end with the settled state and `move` fires only on real moves. Sampling on `resize` saves an animation frame as the user's size; sampling only in a debounce loses a moved rectangle when a maximise follows within the debounce. Adjacent, also measured: `maximize()` shows a hidden window by itself, and a window requested below `minWidth`/`minHeight` comes out at the minimums (800×500 → 1024×640). | Sample on the settled events; let `resize` only mark a sample as owed for the debounced write; take bounds only from a sample that is neither maximised, full screen nor minimised, and leave the `maximized` flag alone while full screen (`foldWindowSample` in `window-state.ts`). | window-state, src/main/index.ts |
| G79 | **The JS `fs.realpathSync` keeps the case a path was TYPED in on a case-insensitive volume; `realpathSync.native` returns the case on disk.** Measured on APFS (case-insensitive by default): `/Users/me/Code/x` and `/Users/me/code/x` stay two different strings under the JS version, so a "same real path" check says one directory is two. | Use `realpathSync.native` for identity comparisons, with a lexical `resolve` fallback when the path does not exist (`safeRealpath` in `repo-discovery.ts`). | repo-discovery |
| G80 | **Reading a file inside a repository someone else wrote can leak a secret or freeze the main process.** Both measured while building Plan 06's repo hints: a `README.md` symlinked to `~/.netrc` put its contents into a hint bound for a model prompt, and opening a FIFO without `O_NONBLOCK` blocks until a writer appears — measured freezing `discoverRepos`, and so Electron's main process. | Confine by REAL path (the file strictly under the repository's real path), open with `O_RDONLY \| O_NONBLOCK \| O_NOFOLLOW`, `fstat` the descriptor and skip anything that is not a regular file, and read a bounded head (G63). `readRepoFile` in `repo-discovery.ts` is the reference. | repo-discovery |
| G81 | **Linear's MCP `list_issues` returns the human identifier as `id`, and the UUID as `uuid`.** Measured 2026-09-16 against `https://mcp.linear.app/mcp`: `result.content[0].text` parses to `{"issues":[{id:"AC-3462", uuid:"0d6b…", title, description, …}]}`. Every instinct says `id` is the primary key and `identifier` is the display string; here it is the other way round, and `get_issue` (Plan 06) does the same. A reader that trusts `id` as an opaque key puts `AC-3462` in a UUID column, and one that trusts it as a UUID puts a UUID in the sidebar. Three neighbours are the same kind of trap, all measured on the same day: the state's name is **`status`** and its machine type **`statusType`** (there is no `state` field at all, though `list_issues` takes a `state` PARAMETER), the team arrives as a **name** in `team` with the id in `teamId` — `list_teams` returns **no key**, so there is no `AC` to show anywhere — and a row carries **`cycleId` and no cycle number**, so `cycle 32` costs a second `list_cycles` call or is simply not shown. | Run the identifier through the thing that already decides what a ticket reference is (`parseLinearRef`) and believe it only if that says yes; drop the row otherwise. `readIdentifier` in `shared/linear-issues.ts` is the reference. For the rest: enumerate the payload's real keys once and write the reader to those, rather than to the names the tool's own parameter list implies. | shared/linear-issues |
| G82 | **A streamable-HTTP MCP server can answer the same request as plain JSON or as SSE, and may return no `Mcp-Session-Id` at all.** Measured against Linear's server: `initialize` came back 200 with `serverInfo` and **no session header**, so the server is stateless — there is no session to carry, resume or invalidate — and a later `tools/call` answered correctly with the handshake sent only once per process. Bodies arrived in both shapes for identical requests. A client that reads `JSON.parse(body)` alone breaks the first time the server chooses SSE; one that requires a session id never gets started. | Send `Accept: application/json, text/event-stream` and parse both: take the first `data:` line that is a JSON-RPC reply (skipping keep-alives and progress notifications), then, only if none was, join each event's `data:` lines with `\n`; tolerate CRLF and strip exactly ONE space after the colon. Treat the handshake as a formality to cache per process, cached only once it has SUCCEEDED, and send it with no caller's abort signal — one dialog closing must not fail the next caller. Never refresh a borrowed OAuth token: rotating it breaks the application you borrowed it from. `src/main/services/linear-mcp.ts` is the reference. | linear-mcp |
| G83 | **An MCP tool REFUSAL arrives as HTTP 200 with a normal `result` — `result.isError === true` — not as a JSON-RPC `error`; and when there IS a JSON-RPC error, its CODE is what says whether the method ran.** Measured 2026-09-16: `list_cycles` with an unrecognised `limit` came back 200, `isError: true`, text `Input validation error: Invalid arguments for tool list_cycles: Unrecognized key: "limit"`. A client that only inspects the JSON-RPC `error` member reads that refusal as a successful empty answer. Two traps ride along. (1) **Sibling tools do not share a parameter schema** — `list_teams` takes `limit` and `list_cycles` rejects it — so an argument that works on one is a hard error on the next; read `tools/list` rather than assuming. (2) For a WRITE, `-32700/-32600/-32601/-32602` are decided before the method runs (nothing happened, safe to resend), while `-32603`, the server-defined `-32000..-32099` range, an error with no code, a non-OK status and an unreadable 200 body all mean the request was SENT and its outcome is unknown. | Check `isError` before reading `content[0].text`, on tool calls **and** on the handshake (an unchecked refusal there gets cached as a success for the app run). Record "refused" vs "unknown" on the error object where it is known, rather than inferring it later from a message or a code, and never auto-retry a write whose outcome is unknown — offer the user a way to go and LOOK instead. `LinearError.outcome` and `LINEAR_CREATE_UNCONFIRMED` are the reference. | linear-mcp, ipc/handlers |
| G84 | **Electron main's global `fetch` is Node's undici: it ignores the system proxy and the OS certificate store, and a GUI-launched app has no environment to tell it otherwise.** undici reads no proxy variable without `NODE_USE_ENV_PROXY` and trusts only its own bundled CA list, so behind a corporate proxy or a TLS-inspecting CA every request fails — and the app cannot fall back on `HTTPS_PROXY`/`NODE_EXTRA_CA_CERTS` the way a terminal can, because an app started from the Dock or Finder inherits no shell environment at all (the same fact G4 records for `PATH`). The failure looks like the user's own connection being down. | Use Electron's `net.fetch`, which goes through Chromium's network stack and so picks up both. Touch `net` lazily (inside the call, not at module load) so nothing runs before `app.whenReady()`. Two knowing trade-offs: it uses the DEFAULT session, so the window's `onHeadersReceived` sees the replies and its cookie jar is shared (`session.fromPartition` is the escape hatch); and its documented limitations are `data:`/`blob:` schemes, an ignored `integrity` and a wrong `.type`/`.url` on the Response — `signal` IS honoured. Inject it rather than reaching for it, so tests never touch the network. | src/main/index.ts, linear-mcp |
| G85 | **zod 4.5's `.max()` on a string counts CODE POINTS, not UTF-16 units.** Measured on this tree: `z.string().max(250)` accepts a 250-emoji string, which is 500 UTF-16 units, and rejects the 251st. So any hand-rolled length check, cap or inline form validation written with `.length` disagrees with the schema at the boundary — the form says the value is fine and the IPC call comes back an opaque `BAD_REQUEST`, or the reverse. Slicing by units has a second cost: it can cut a surrogate pair in half. | Count and cut with `Array.from(s)` everywhere the same value is bounded (`countChars`/`capChars` in `shared/linear-issues.ts`), and make the cleaner IDEMPOTENT if it runs more than once on the same text — `.trimEnd()` AFTER the cap as well as `.trim()` before it, or a 20 000-character description loses one character per pass (measured). For a hostile answer, pre-slice by units (`s.slice(0, max * 2)`) before `Array.from`, so a 4 MB string is not expanded into a four-million-element array. | shared/linear-issues, shared/ipc-schemas |
| G86 | **A Linear cycle boundary is an INSTANT at the team's local midnight, and `endsAt` is EXCLUSIVE.** Measured 2026-09-17 against a UTC+2 team: cycle 33, which Linear itself labels 29 Sep – 12 Oct, carries `startsAt: "2026-09-28T22:00:00.000Z"` — so the UTC day of that instant is the 28th, and so is its local day in London. And cycle 32's `endsAt` is the IDENTICAL instant, so printing the end day as it stands ends one cycle on the day the next one begins. A constant offset is not the fix: the same boundary becomes 23:00Z once Europe leaves DST on 25 Oct 2026, and a fixed correction would be a day out from then on. | Snap the instant to the MIDDLE of its day (`ms + 12 h`) before formatting — that lands inside the intended day for every zone from UTC-11 to UTC+11 and survives the DST move — then read `getUTC*` with a month table, never `toLocaleDateString`, so a test can assert the string. Give the END its own formatter that subtracts a day (`startDayLabel`/`endDayLabel` in `shared/linear-cycle-run.ts`), because one shared formatter is exactly the edit that puts the exclusive day back. Compare windows half-open, `from <= now < to`, so the shared instant belongs to the later cycle and never to both. | shared/linear-cycle-run |
| G87 | **A loop that runs for minutes must re-read the store between iterations: a subscribed value is as old as the last commit.** `const agents = useWorkspace((s) => …)` read at the top of a component is the list as it was when React last committed, and a whole-cycle run does its work BETWEEN renders — so an agent created by another window, or by the run's own previous iteration, is invisible to the very guard that exists to stop a second one being made. The same trap applies to any long-running handler that reads store state it also causes to change. | Re-read with `useWorkspace.getState()` immediately before the decision, and keep the subscribed value for what is DRAWN. `CycleRun.tsx`'s `agentExists` is the reference — it is called both before the look-up and again immediately before the create, because the look-up itself is half a minute long; `create-agent.ts`'s `projectFor`/`folderNamed` read the same way, which is what makes one folder per run true without the run tracking anything. | CycleRun, create-agent |
| G88 | **`vitest.config.ts` pins `TZ=UTC` for the whole suite, so a local-versus-UTC formatting bug passes on every machine.** Measured while G86's bug was live: replacing `getUTCDate()` with `getDate()` in the cycle label changed nothing under the pinned zone — and nothing under `America/New_York`, `Europe/London` or `Asia/Tokyo` either, because a midday snap puts the instant at noon in the TEAM's zone and any zone within twelve hours of it still prints the right day. The suite was green with the bug in it. | To pin a date FORMAT, set `process.env.TZ` inside the test and restore it in an `afterEach`, and pick zones far enough out to break it: `Pacific/Kiritimati` (UTC+14) and `Pacific/Midway` (UTC-11) are 12 and 13 hours from a UTC+2 team, which is where the local day finally differs. A green test under the config's pinned zone is evidence about that zone and nothing else. | vitest.config.ts, shared/linear-cycle-run |
| G89 | **`agent:progress` is a BROADCAST with no addressing, so two overlapping creates donate each other's events.** Every listener sees every operation's steps. It did not matter while one dialog was the only caller; the whole-cycle run is strictly sequential but the New Agent dialog stays reachable while it runs, and a dialog create landing mid-run handed its `saved` step and its agent id to the run's own recorder — which would make the run refuse a Retry that is safe, and offer an agent it never created. `agent-service.ts`'s `progressFor` already mints one `opId` per operation and stamps it on every event. | Bind the recorder to exactly ONE `opId`: claim the first event whose `opId` no other in-flight sequence holds, ignore every event that does not carry it, and release both the subscription and the claim in a `finally` — a listener left subscribed outlives the sequence that made it, and a claim left standing makes the NEXT create skip its own events and report `saved: false` for an agent that exists. `RECORDING_OPS` in `src/renderer/lib/create-agent.ts` is the reference. | create-agent |
| G90 | **macOS attributes a microphone request to the responsible APP, so a `claude` running in a Hangar pane can never hold a microphone grant — Claude Code's own voice mode cannot work in any Hangar pane.** Measured for spec 2026-09-18 §1: the process asking is `claude`, a child of a login shell, a child of the session host, which launchd has detached from the app. macOS finds no responsible app to attribute the request to and denies it without ever showing a prompt, so there is nothing to click and nothing in the terminal that can fix it. The mechanism is the process tree, not Claude Code, so any other CLI in a pane that asks for the microphone would meet the same denial. | Do the listening in Hangar itself. `hangar-dictate` is spawned by Electron MAIN (`src/main/services/dictation.ts`), never by the session host, so the request is Hangar's: Hangar.app's Info.plist supplies the prompt's sentence and its signature holds the grant, which is why that signature has to be stable (spec §5, G93). Launch the app through LaunchServices — `open -a Hangar`, Finder, the Dock — not `nohup`, or macOS has no foreground app to attribute the prompt to. | src/main/services/dictation, mac/dictate |
| G91 | **xterm.js 6.0.0 has no kitty keyboard protocol, so no terminal in Hangar can report a key RELEASE — and Hangar sets no `TERM_PROGRAM`, so a CLI cannot identify the terminal either.** Measured for spec 2026-09-18 §1: zero occurrences of the protocol in `@xterm/xterm`'s `xterm.js`, and `agent-service.ts` launches every agent with `TERM=xterm-256color`, `COLORTERM=truecolor` and no `TERM_PROGRAM` at all. So anything that works by HOLDING a key — Claude Code's hold-to-talk voice mode is the case that found this — never sees the key come up, and a CLI that picks behaviour by terminal falls back to its generic path. | Nothing in Hangar may depend on a key-up from the terminal. Dictation is press-to-toggle instead: the mic button in the pane header, or ⌘D, press to start and press again to stop — never a hold. Recheck this row if `@xterm/xterm` is upgraded. | terminal-registry, agent-service |
| G92 | **OpenSSL 3 writes a PKCS#12 that macOS `security import` rejects with "MAC verification failed", even with the right password.** OpenSSL 3's `pkcs12 -export` defaults to PBES2/PBKDF2 with AES-256-CBC and a SHA-256 MAC (measured on OpenSSL 3.6.4: `openssl pkcs12 -info` reports `MAC: sha256` and `PBES2, PBKDF2, AES-256-CBC`), which the macOS importer does not read, and the error it gives names the MAC rather than the format — so it reads as a mistyped password. Homebrew's `openssl` is 3.x and comes first on `PATH`; `/usr/bin/openssl` is LibreSSL. | Export with the old algorithms spelled out: `-certpbe PBE-SHA1-3DES -keypbe PBE-SHA1-3DES -macalg sha1` (`-info` then reports `MAC: sha1` and `pbeWithSHA1And3-KeyTripleDES-CBC`). The whole certificate recipe is in `docs/RUNBOOK.md` → "A stable signature". | docs/RUNBOOK |
| G93 | **A self-signed certificate that nobody has marked trusted shows as `0 valid identities found` — and still signs.** `security find-identity -v -p codesigning` lists only identities that pass trust evaluation; without `-v` the same certificate is listed as `CSSMERR_TP_NOT_TRUSTED`. electron-builder looks identities up with exactly that `-v` listing, so `mac.identity` can never name one. `codesign` itself signs with it, measured on 2026-09-18 by SHA-1 and by name alike, and the result verifies (`--verify --deep --strict`) and carries a designated requirement of `identifier "dev.hangar.app" and certificate root = H"<sha1>"`, which does not change between builds. Two traps ride along. By name, codesign takes an identity preference of that name if there is one, else any certificate whose common name CONTAINS the string, so what a name signs with depends on the rest of the keychain. And **`codesign -dv` prints no `Authority=` line at all** for a certificate-signed file: it takes `-dvv`, so a check that reads `-dv` can never see who signed. | Keep `identity: null` in `electron-builder.yml` and sign in `scripts/sign.mjs`: resolve the name to its SHA-1 with `security find-certificate -a -c <name> -Z`, matching the `labl` exactly, hand codesign only the hash, and read the authority back with `codesign -d --verbose=2`. No trust setting is needed. `scripts/signing.ts` is the reference. | scripts/sign, scripts/signing |
